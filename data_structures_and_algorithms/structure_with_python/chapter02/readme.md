# 算法的时间复杂度 一般情况下

时间复杂度的相对关系

f(1) > f(log n) > f(n) > f(n log n) > f(n ^ 2) > f(n ^ 3) > f(2 ^ n)

n^2.ipynb 乱序字符串检查最差的一种实现方式

O(n^2)_2.ipynb 乱序字符串检查另外的最差的一种实现方式

O(n).ipynb 乱序字符串检查比较好的一种实现方式，牺牲了2倍的空间

